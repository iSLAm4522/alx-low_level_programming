#include "main.h"
/**
* _abs - computes the absolute value of an integer.
* @n: The number to compute absolute value for it.
* Return: absolute value for n.
*/
int _abs(int n)
{
if (n > 0)
return (n);
else
return (-n);
}
